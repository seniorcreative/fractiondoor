/**
 * Central lucide-react icon lookup.
 *
 * Unit presets, lessons and a couple of hardcoded UI bits reference icons by
 * name (a string) rather than importing components directly, so the data
 * files in `lessons/` and `units.js` stay framework-agnostic. Components
 * resolve the name through `ICONS` at render time.
 */

import {
  Square,
  Pizza,
  CakeSlice,
  Grid3x3,
  PoundSterling,
  Clock,
  Calendar,
  Ruler,
  Scale,
  FlaskConical,
  Compass,
  Percent,
  Users,
  Egg,
  Sparkles,
  Microscope,
  PartyPopper,
} from 'lucide-react';

export const ICONS = {
  Square,
  Pizza,
  CakeSlice,
  Grid3x3,
  PoundSterling,
  Clock,
  Calendar,
  Ruler,
  Scale,
  FlaskConical,
  Compass,
  Percent,
  Users,
  Egg,
  Sparkles,
  Microscope,
  PartyPopper,
};
